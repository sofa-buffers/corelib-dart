include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true
  errors:
    # The hot paths use deliberate bitwise / integer idioms.
    unused_element: warning

linter:
  rules:
    - prefer_final_locals
    - avoid_print
